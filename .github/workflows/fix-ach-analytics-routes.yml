name: Fix ACH Analytics Routes
on:
  push:
    branches: [main]

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Read api-routes.cjs and try filemng upload
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 74.208.191.205
          username: dlbtrust.cloud_h6zkwzag1ya
          password: Deploy2026!
          port: 22
          command_timeout: 180s
          script: |
            export HOME=/var/www/vhosts/dlbtrust.cloud
            echo "=== Read api-routes.cjs ==="
            cat /var/www/vhosts/dlbtrust.cloud/dlbtrust-v2/api-routes.cjs
            echo "=== End ==="
